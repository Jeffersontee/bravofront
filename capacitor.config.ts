import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.livecodesystem.bravoinstalacoes',
  appName: 'Bravo Instalações',
  webDir: 'www'
};

export default config;
